{
  "name": "@sfcr/chat-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "deploy": "pnpm dlx wrangler deploy",
    "dev": "node scripts/dev-node.mjs",
    "dev:wrangler": "pnpm dlx wrangler dev",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
